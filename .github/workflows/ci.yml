name: CI/CD

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-checks:
    name: Backend · lint & tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:12.0-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: fantasy_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      SECRET_KEY: ci-only-secret-key
      DEBUG: "True"
      PG_ENGINE: django.db.backends.postgresql
      POSTGRES_DB: fantasy_test
      PG_USER: postgres
      PG_PASSWORD: postgres
      PG_HOST: 127.0.0.1
      PG_PORT: "5432"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.10" }
      - run: pip install --upgrade pip && pip install -r requirements.txt
      - name: flake8
        run: flake8 .
      - name: black (check)
        run: black --check --exclude='migrations|venv|client' .
      - name: isort (check)
        run: isort . --check-only --skip env --skip migrations --skip .venv --skip client
      - name: pytest
        run: |
          set +e
          pytest -p no:warnings --cov=.
          code=$?
          set -e
          # pytest exits 5 when zero tests are collected; treat as pass for now
          if [ "$code" -eq 5 ]; then echo "No tests collected — skipped."; exit 0; fi
          exit $code

  frontend-build:
    name: Frontend · type-check & build
    runs-on: ubuntu-latest
    defaults:
      run: { working-directory: client }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: client/package-lock.json
      - run: npm ci
      - run: npm run build   # vue-tsc type-check + vite build

  auto-fix:
    name: Auto-fix formatting (black & isort)
    needs: [backend-checks, frontend-build]
    if: failure()          # only runs when a check failed
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with: { python-version: "3.10" }
      - run: pip install black==24.8.0 isort==5.13.2
      - run: black --exclude='migrations|venv|client' .
      - run: isort . --skip env --skip migrations --skip .venv --skip client
      - name: Commit auto-fixes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "style: auto-format code with black & isort"
          commit_user_name: "github-actions[bot]"
          commit_user_email: "41898282+github-actions[bot]@users.noreply.github.com"

  deploy:
    name: Deploy to Contabo VPS
    needs: [backend-checks, frontend-build]
    if: (github.event_name == 'push' && github.ref == 'refs/heads/master') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Pull, rebuild and restart on the VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || '22' }}
          script: |
            set -e
            cd "${{ secrets.VPS_APP_DIR }}"
            git pull --ff-only origin master
            docker compose -f docker-compose.prod.yml --env-file .env.prod up --build -d --remove-orphans
            docker compose -f docker-compose.prod.yml --env-file .env.prod exec -T api python3 manage.py migrate --no-input
            docker compose -f docker-compose.prod.yml --env-file .env.prod exec -T api python3 manage.py collectstatic --no-input --clear